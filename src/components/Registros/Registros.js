import { StyledContainerRegistros } from "./styled";

const Registros = () => {
    return (
        <StyledContainerRegistros>
            <h1>Não há Registros de entrada ou saída</h1>
        </StyledContainerRegistros>
    );
};

export default Registros;
